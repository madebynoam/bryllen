import type { ProjectManifest } from 'bryllen/runtime'

// TODO: Update component imports after migration
// Old structure had components in v1/, v2/, etc. folders

const manifest: ProjectManifest = {
  id: '25ad55f6-27f8-493e-8263-b012818af79a',
  project: 'landing-page',
  frames: [],
  grid: {
    columns: 4,
    columnWidth: 320,
    rowHeight: 200,
    gap: 40,
  },
}

export default manifest
